<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_budget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_card_background"
    android:padding="16dp">

    <TextView
        android:id="@+id/widget_budget_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Monthly Budget"
        android:textColor="#78909C"
        android:textSize="12sp"
        android:fontFamily="sans-serif-medium"
        android:textAllCaps="true"
        android:letterSpacing="0.05"/>

    <TextView
        android:id="@+id/widget_budget_remaining"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:text="Remaining: Rp 0"
        android:textColor="#0288D1"
        android:textSize="12sp"
        android:fontFamily="sans-serif-medium"/>

    <ProgressBar
        android:id="@+id/widget_budget_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="8dp"
        android:layout_below="@id/widget_budget_title"
        android:layout_marginTop="12dp"
        android:progressDrawable="@drawable/widget_progress_bar"
        android:max="100"
        android:progress="0"/>

    <TextView
        android:id="@+id/widget_budget_spent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/widget_budget_progress"
        android:layout_marginTop="8dp"
        android:text="Rp 0 used"
        android:textColor="#37474F"
        android:textSize="14sp"
        android:textStyle="bold"/>

    <TextView
        android:id="@+id/widget_budget_percent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/widget_budget_progress"
        android:layout_toEndOf="@id/widget_budget_spent"
        android:layout_marginTop="8dp"
        android:layout_marginStart="4dp"
        android:text="(0%)"
        android:textColor="#37474F"
        android:textSize="14sp"/>

    <TextView
        android:id="@+id/widget_budget_total"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/widget_budget_progress"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="8dp"
        android:text="/ Rp 0"
        android:textColor="#90A4AE"
        android:textSize="12sp"/>

</RelativeLayout>
